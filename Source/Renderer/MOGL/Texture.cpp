#include "Texture.hpp"

Texture* Texture::newTextureWithDescriptor(TextureDescriptor *descriptor, int offset, int bytesPerRow) {
	return nullptr;
}

void* Texture::contents() {
	return nullptr;
}

Texture* Texture::newTextureViewWithPixelFormat(PixelFormat *format) {
	return nullptr;
}

Resource* Texture::rootResource() {
	return nullptr;
}